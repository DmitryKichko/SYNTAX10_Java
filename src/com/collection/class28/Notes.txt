Абстракция:
Процесс сокрытия деталей реализации и демонстрации пользователю только функциональности
пользователю называется абстракцией в java:
Мы можем достичь абстракции в java, используя абстрактные классы и интерфейсы.

Абстракция:
Абстрактные классы - это классы с ключевым словом abstract
1) мы можем иметь определенные и неопределенные методы в абстрактных классах.
2) мы не можем создать объект абстрактного класса напрямую, но можем создать его косвенно с помощью наследования.

Интерфейсы:
В java все классы, реализующие интерфейс, должны предоставлять реализацию абстрактных методов в этом интерфейсе
(которые определены в интерфейсе), и этот класс должен быть объявлен абстрактным.

Свойства интерфейса:
- Все переменные являются публичными статическими и конечными
- Все методы по умолчанию являются общедоступными абстрактными

Инкапсуляция:
Инкапсуляция в java - это сокрытие полей класса, делая их приватными и предоставляя через методы
getter и setters для чтения и записи этих полей.

Инкапсуляция обеспечивает безопасность, мы можем написать различные шаги проверки, прежде чем
присвоения значения полям класса. Мы также можем написать логику для предоставления
ограниченный доступ к этим полям.

================================================================================================================
Проблема с примитивными переменными они могут одновременно хранить только одно значение.
int a=10;
Мы можем решить эту проблему с помощью массивов.
Массивы могут хранить несколько значений в одной переменной.

Проблемы с массивами:
- Фиксированный размер


HW:

1. Создайте массив автомобилей и извлеките из него все
значения, используя 3 различных способа.

2. Создайте массивСписок слов. Удалите каждое
слово, которое заканчивается на "е".

3. Создайте массивСписок напитков. Если в каком-либо напитке есть
буква "а" или "е", замените его на воду.

4. Создайте массивСписок четных чисел от 1
до 50. Удалите из этого списка любое число, кратное
на 5 из этого списка.